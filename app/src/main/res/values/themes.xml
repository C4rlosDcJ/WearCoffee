<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WearCoffee" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/sakura_primary</item>
        <item name="colorPrimaryDark">@color/sakura_primary_dark</item>
        <item name="colorAccent">@color/sakura_accent</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg_gradient_start</item>
        <item name="android:windowBackground">@color/bg_gradient_end</item>
    </style>
</resources>